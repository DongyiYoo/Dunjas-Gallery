<!DOCTYPE html>
<html>
<head>
    <title>Login</title>
    <style>
        body { font-family: sans-serif; padding: 50px; text-align: center; }
        form { display: inline-block; border: 1px solid #ddd; padding: 20px; border-radius: 10px; }
        input { margin: 5px 0; padding: 5px; width: 200px; }
        button { padding: 5px 15px; cursor: pointer; background-color: #007bff; color: white; border: none; }
        .error { color: red; font-weight: bold; }
    </style>
</head>
<body>
    <h2>Login</h2>

    <% if (error) { %>
        <p class="error"><%= error %></p>
    <% } %>

    <form action="/login" method="POST">
        <input type="text" name="username" placeholder="Username" required><br>
        <input type="password" name="password" placeholder="Password" required><br><br>
        <button type="submit">Login</button>
    </form>

    <br><br>
    <p>Don't have an account yet? </p><a href="/signup">Sign up here</a>
    <br><br>
    <a href="/">Back to Gallery</a> 
</body>
</html>